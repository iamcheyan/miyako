# Ralph Progress Log - NasMusicSync

## Codebase Patterns
- Use ESLint flat config format (eslint.config.js) for new projects
- Tauri 2.0 requires system dependencies on Linux: pkg-config, libglib2.0-dev, libgtk-3-dev, libwebkit2gtk-4.1-dev, libayatana-appindicator3-dev, librsvg2-dev
- Icons must be RGBA format PNG for Tauri
- Run `rustup default stable` to set up Rust toolchain

---

## 2026-05-30 08:30 - US-001
- What was implemented: Project scaffolding with Tauri 2.0 + React + TypeScript
- Files changed:
  - package.json (project configuration)
  - vite.config.ts (Vite configuration)
  - tsconfig.json, tsconfig.node.json (TypeScript configuration)
  - eslint.config.js (ESLint flat config)
  - .prettierrc (Prettier configuration)
  - index.html (entry point)
  - src/main.tsx, src/App.tsx (React components)
  - src/index.css, src/App.css (styles)
  - src-tauri/Cargo.toml (Rust dependencies)
  - src-tauri/tauri.conf.json (Tauri configuration)
  - src-tauri/src/lib.rs, src-tauri/src/main.rs (Rust entry points)
- **Learnings for future iterations:**
  - ESLint flat config format is required for new projects (not the old .eslintrc)
  - Tauri 2.0 on Linux requires system GTK/WebKit dependencies
  - Icons must be RGBA PNG format, not just RGB
  - Use `cargo check` to verify Rust compilation
  - Run `npm run typecheck` and `npm run lint` before committing
- The `smb` crate (v0.11) requires exactly one runtime feature: `async`, `single_threaded`, or `multi_threaded`; default includes `async`
- `smb` re-exports `smb_fscc::*` (FileDirectoryInformation, FileAccessMask, etc.) and `smb_dtyp::*` (FileTime)
- `Directory::query` takes `&Arc<Self>` (not `&self` or `Arc<Self>`) — must use `Directory::query::<T>(&arc, pattern)`
- `FileTime` has `.date_time()` → `PrimitiveDateTime`, which needs `.assume_utc().unix_timestamp()` for unix timestamps
- `File::get_len()` requires importing `smb::GetLen` trait
- `Resource` enum variants (`File`, `Directory`) deref to `ResourceHandle` via `Deref` impl
- `resource.unwrap_dir()` consumes the resource to get owned `Directory`; `resource.as_dir()` returns `&Directory`
- `futures-util` is needed for `StreamExt` to iterate async streams
---

## 2026-05-30 - US-002
- What was implemented: SMB client Rust backend with Tauri commands
- Files changed:
  - src-tauri/Cargo.toml (added futures-util dependency)
  - src-tauri/src/smb_client.rs (complete rewrite with correct smb crate API)
- **Learnings for future iterations:**
  - The `smb` crate's `Directory::query` signature is `fn query<'a, T>(this: &'a Arc<Self>, pattern: &str)` — not a method on `&self`
  - `FileTime::date_time()` returns `PrimitiveDateTime`; use `.assume_utc().unix_timestamp()` for i64 timestamps
  - `Resource` enum doesn't implement `Deref` directly — use `as_file()`/`as_dir()` or pattern matching
  - `smb::GetLen` trait must be imported to call `get_len()` on `File`
  - Connection manager stores `Client` per connection (each client can hold multiple share connections)
  - `Client::close()` closes all managed connections/trees/sessions
---

## 2026-05-30 - US-003
- What was implemented: Settings page for SMB server configuration
- Files changed:
  - src/components/Settings.tsx (SMB config form with inputs, password toggle, test connection)
  - src/components/Settings.css (dark theme styling)
  - src/types/tauri-commands.ts (TypeScript type definitions for Tauri commands)
- **Learnings for future iterations:**
  - Use `localStorage` for persisting SMB config between app sessions
  - Password input toggle uses `type="text"` vs `type="password"` switch
  - Tauri `invoke` returns errors as thrown exceptions, catch with try/catch
  - Use `useState` with underscore prefix (`[, setConnectionId]`) when state setter needed but value unused
---

## 2026-05-30 - US-005
- What was implemented: Sync engine core logic with recursive directory scanning and file comparison
- Files changed:
  - src-tauri/src/sync_engine.rs (new: sync engine with scan, compare, download functions)
  - src-tauri/src/lib.rs (added sync_* Tauri commands)
  - src/types/tauri-commands.ts (added RemoteFile, SyncAction, SyncState types)
- **Learnings for future iterations:**
  - Use `Box::pin()` for recursive async functions
  - Sync state is persisted as JSON with last_sync_time and synced_files array
  - Music file detection uses case-insensitive extension matching (.mp3, .flac, .aac, .wav)
  - File comparison checks both size and last_modified timestamp
---

## 2026-05-30 - US-006
- What was implemented: Sync page UI with progress tracking and sync log
- Files changed:
  - src/components/SyncPage.tsx (sync management page with start button, progress bar, log)
  - src/components/SyncPage.css (dark theme styling)
  - src/App.tsx (added SyncPage component)
- **Learnings for future iterations:**
  - Sync state is loaded on component mount and updated after sync completes
  - Progress bar shows current/total files being downloaded
  - Sync log shows timestamped messages with color coding (info/success/error)
  - Button is disabled while syncing or when not connected
---

## 2026-05-30 - US-007
- What was implemented: Local music library display with folder navigation and search
- Files changed:
  - src/components/MusicLibrary.tsx (music library with folder grid, file list, search)
  - src/components/MusicLibrary.css (dark theme styling)
  - src/App.tsx (added MusicLibrary component)
- **Learnings for future iterations:**
  - Music files are grouped by folder from sync state
  - Folder view shows folder name and file count
  - File list shows formatted name (without extension) and size
  - Search filters files by name (case-insensitive)
  - Empty state shows guidance to sync first
---

## 2026-05-30 - US-008
- What was implemented: Audio playback engine with play queue and play modes
- Files changed:
  - src/lib/audioPlayer.ts (AudioPlayer class with HTML5 Audio API wrapper)
- **Learnings for future iterations:**
  - AudioPlayer is a singleton class with event-based architecture
  - Supports three play modes: sequential, loop, shuffle
  - Playlist management with next/previous/seekTo controls
  - Event listeners for play, pause, stop, ended, timeupdate, loadedmetadata, error
  - Use `getAudioPlayer()` to get singleton instance
---

## 2026-05-30 - US-009
- What was implemented: Player UI with mini player, expanded view, and playlist
- Files changed:
  - src/components/PlayerUI.tsx (player UI with mini/expanded views, controls, playlist)
  - src/components/PlayerUI.css (dark theme styling)
  - src/App.tsx (added PlayerUI component)
- **Learnings for future iterations:**
  - Mini player shows track name and time, click to expand
  - Expanded player shows full controls, progress bar, volume, playlist
  - Play mode cycles through sequential → loop → shuffle
  - Playlist shows all tracks with active highlight
  - Player is fixed at bottom of screen
---

## 2026-05-30 - US-010
- What was implemented: MediaSession integration for lock screen and notification controls
- Files changed:
  - src/lib/mediaSession.ts (MediaSession manager with metadata and action handlers)
  - src/App.tsx (initialize MediaSession on app start)
- **Learnings for future iterations:**
  - MediaSession API provides lock screen and notification bar controls
  - Set metadata (title, artist) for each track
  - Action handlers for play, pause, stop, previoustrack, nexttrack, seekto
  - Position state syncs with audio playback
  - Initialize MediaSession once on app mount
---

## 2026-05-30 - US-011
- What was implemented: Local data persistence for recent tracks, history, and playback state
- Files changed:
  - src/lib/storage.ts (StorageManager class for JSON-based persistence)
  - src-tauri/src/lib.rs (added storage_read and storage_write commands)
  - src-tauri/Cargo.toml (added dirs crate for app data directory)
- **Learnings for future iterations:**
  - Storage uses Tauri app data directory (~/nas-music-sync/)
  - Three JSON files: recent.json (last 50), history.json (all), playback_state.json
  - StorageManager is a singleton with async initialize() method
  - Recent tracks are sorted by playedAt (newest first)
  - Playback state saves current track, time, volume, play mode
---

## 2026-05-30 - US-012
- What was implemented: Bottom navigation with page routing
- Files changed:
  - src/App.tsx (added BrowserRouter, Routes, NavLink for navigation)
  - src/App.css (updated styles for bottom navigation)
  - package.json (added react-router-dom dependency)
- **Learnings for future iterations:**
  - Use react-router-dom for client-side routing
  - Bottom navigation has 3 tabs: 音乐库 (/), 同步 (/sync), 设置 (/settings)
  - NavLink with isActive for active tab highlighting
  - Player is fixed above bottom navigation
  - Content area has padding-bottom to avoid overlap with player and nav
---

## 2026-05-30 - US-013
- What was implemented: Sync state persistence and conflict handling
- Files changed:
  - src-tauri/src/sync_engine.rs (updated sync_download to get accurate file info)
- **Learnings for future iterations:**
  - Sync state persists across app restarts via sync_manifest.json
  - Local file deletion triggers re-download on next sync
  - Remote file deletion keeps local files (no auto-delete)
  - File info is fetched after download for accurate size and timestamp
  - Incremental sync compares size and last_modified timestamp
---

## 2026-05-30 - US-014
- What was implemented: Error handling and user feedback with Toast notifications
- Files changed:
  - src/components/Toast.tsx (Toast component with useToast hook)
  - src/components/Toast.css (dark theme styling)
  - src/App.tsx (added Toast component)
- **Learnings for future iterations:**
  - Toast supports 4 types: success, error, warning, info
  - Auto-dismiss after 3 seconds (configurable)
  - useToast hook returns messages, addToast, removeToast
  - Toast container is fixed at top-right corner
  - Animation slides in from right
---
